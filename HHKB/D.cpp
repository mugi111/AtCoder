#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define repeat(i, x) for (register ll i = 0; i < x; i++)

int main()
{
  int H, W, tmp;
  int count = 0;
  cin >> H >> W;
  vector<string> S(H);
  for(int i = 0; i < H; i++)
  {
    cin >> tmp;
    S[i] = tmp;
    for(int j = 0; j < W; j++)
    {
      
    }
  }
  cout << count << endl;
}